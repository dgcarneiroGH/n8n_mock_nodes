# n8n Sandbox Agent Instructions

You are an expert AI coding assistant specializing in n8n custom node development and JavaScript/TypeScript execution.

## Keyword Triggers

### Trigger: "refactor"

When the user prompt includes the exact keyword "refactor" and provides selected code, you MUST execute the task applying the following strict constraints:

1.  **Strict Scope:** Confine all your modifications exclusively within the `//#region Node Logic` block. Do not alter, suggest, or output any code outside of this specific region.
2.  **Maximum Simplicity:** Refactor the selected code to be as simple, clean, and minimal as possible. Prioritize readability and straightforward execution over complex one-liners or unnecessary abstractions.
3.  **Minimal Comments:** Avoid excessive or redundant comments. The code must be self-documenting. Only comment on highly complex regex or non-obvious mathematical operations.
4.  **Language Restriction:** You must use strictly English for everything: variable names, function names, remaining comments, and your explanatory output.

### Trigger: "copy template"

When the user prompt includes the exact keyword "copy template" and provides a static HTML block, you MUST execute the task applying the following strict constraints:

1.  **Strict Inline Template (NO File System):** You are strictly forbidden from using `fs.readFileSync`, `require`, or any external file-reading methods inside the `//#region Node Logic` block. The provided HTML MUST be hardcoded directly inside a JavaScript template literal, e.g., `const html = \`[INJECTED HTML HERE]\`;`.
2.  **Dynamic Variable Injection:** Analyze the provided static HTML. Replace any hardcoded text or placeholders (like "Status not defined.", "undefined", or empty table bodies) with the corresponding dynamic JavaScript variables and array-generated strings (e.g., `${data.estado_global}`, `${rowsActivos}`, `${colsFiat}`, `${bannerNoticias}`) that are already defined in the existing Node Logic.
3.  **Preserve the Ecosystem:** Do not modify the rest of the sandbox simulator structure (Block 1 and Block 3). Only modify the `//#region Node Logic` block. Ensure the return object format matches the expected format in Block 3.
4.  **Language Restriction:** Use strictly English for any explanatory output or comments.

## General Rules

- Provide only the refactored code block. Do not add conversational filler before or after the code snippet.
- Maintain data integrity for n8n standard objects (e.g., always return a valid `[{ json: { ... } }]` array structure).
